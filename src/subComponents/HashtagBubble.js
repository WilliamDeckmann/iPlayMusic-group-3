const HashtagBubble = (Fill, Text, Size) => {
    // Skal kunne ændre sig i størrelser og fill/no-fill farve
    
  return (
    <>
      <div>
        <h4>{Text}</h4>
      </div>
    </>
  );
};

export default HashtagBubble;
